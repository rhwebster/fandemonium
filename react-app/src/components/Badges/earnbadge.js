const photoBadgeHelperFunction = (n) => {
    if (n = 1 {
        dispatch(user)
    })
}

const checkForNewBadge = () => {
    dispatch(newBadge({
        id: userId,
        badgeId: badgeId,
    }))
    window.location.reload(true);
};